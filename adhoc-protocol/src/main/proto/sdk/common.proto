syntax = "proto3";

package adhoc.sdk.v1;

option java_package = "io.gitee.songchaolin.adhoc.sdk.grpc.v1";
option java_multiple_files = true;

/**
 * SDK 公共错误响应。
 * error_code 用 ADHOC_* 前缀（见 CLAUDE.md §5 错误码体系）。
 */
message ErrorResponse {
  string error_code = 1;
  string error_message = 2;
}